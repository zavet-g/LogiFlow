# 🚚 LogiFlow - Система логистики доставок

**LogiFlow** - это современная система управления логистикой доставок, построенная на Django с использованием Python-технологий. Проект предоставляет полный набор инструментов для управления доставками, включая веб-интерфейс, REST API и административную панель.

## 🎯 Основные возможности

- **Управление доставками**: Создание, редактирование и отслеживание доставок
- **Веб-отчеты**: Интерактивные отчеты с фильтрами, графиками и таблицами
- **REST API**: Полнофункциональное API для интеграции с внешними системами
- **Административная панель**: Удобное управление данными через Django Admin
- **Авторизация**: Система входа и управления пользователями
- **Контейнеризация**: Готовность к развертыванию в Docker

## 🏗️ Архитектура проекта

```
LogiFlow/
├── delivery_project/          # Основной проект Django
├── deliveries/               # Приложение доставок (основное)
├── users/                    # Приложение пользователей
├── orders/                   # Приложение заказов
├── restaurants/              # Приложение ресторанов
├── docs/                     # Документация проекта
├── static/                   # Статические файлы
├── media/                    # Загружаемые файлы
├── templates/                # HTML шаблоны
├── fixtures/                 # Начальные данные
├── Dockerfile               # Конфигурация Docker
├── docker-compose.yml       # Оркестрация контейнеров
├── pyproject.toml           # Зависимости Poetry
├── Makefile                 # Команды для разработки
└── README.md               # Этот файл
```

## 🚀 Быстрый старт

### Предварительные требования

- Python 3.11+
- PostgreSQL 12+
- Poetry (для управления зависимостями)
- Docker & Docker Compose (опционально)

### Установка и запуск

#### Вариант 1: Локальная разработка

```bash
# Клонирование репозитория
git clone https://github.com/zavet-g/LogiFlow.git
cd LogiFlow

# Установка зависимостей
make install

# Настройка переменных окружения
cp env.example .env
# Отредактируйте .env файл

# Применение миграций
make migrate

# Создание суперпользователя
make superuser

# Запуск сервера
make run
```

#### Вариант 2: Docker (рекомендуется)

```bash
# Клонирование репозитория
git clone https://github.com/zavet-g/LogiFlow.git
cd LogiFlow

# Сборка и запуск контейнеров
make build
make up

# Применение миграций
make d-migrate

# Создание суперпользователя
make d-superuser
```

### Доступ к приложению

- **Веб-интерфейс**: http://localhost:8000/
- **Админ-панель**: http://localhost:8000/admin/
- **REST API**: http://localhost:8000/api/
- **Отчеты**: http://localhost:8000/api/deliveries/report/

## 📊 Основные функции

### 1. Управление доставками

Система поддерживает полный цикл управления доставками:

- **Создание доставок** с указанием всех необходимых параметров
- **Отслеживание статусов** доставки в реальном времени
- **Управление транспортом** и типами грузов
- **Загрузка медиафайлов** (фото, документы)
- **Расчет расстояний** и времени доставки

### 2. Веб-отчеты

Интерактивная система отчетности включает:

- **Фильтры по датам**, типам доставки, грузам и статусам
- **Динамические графики** с использованием Chart.js
- **Сортируемые таблицы** с пагинацией
- **Экспорт данных** в различных форматах

### 3. REST API

Полнофункциональное API для интеграции:

```bash
# Получение списка доставок
GET /api/deliveries/deliveries/

# Создание новой доставки
POST /api/deliveries/deliveries/

# Получение данных для отчетов
GET /api/deliveries/api/delivery-data/
```

## 🛠️ Технологический стек

### Backend
- **Django 4.2.7** - основной веб-фреймворк
- **Django REST Framework** - API
- **PostgreSQL** - база данных
- **Poetry** - управление зависимостями
- **Pytest** - тестирование

### Frontend
- **Django Templates** - серверный рендеринг
- **Material Design** - UI компоненты
- **Chart.js** - визуализация данных
- **Vanilla JavaScript** - интерактивность

### DevOps
- **Docker** - контейнеризация
- **Docker Compose** - оркестрация
- **Make** - автоматизация команд

## 📁 Структура приложений

### deliveries/ - Основное приложение
- `models.py` - модели данных доставок
- `views.py` - представления и API
- `admin.py` - административная панель
- `serializers.py` - сериализаторы API
- `templates/` - HTML шаблоны

### users/ - Пользователи
- Кастомная модель пользователя
- Система авторизации
- Управление профилями

### orders/ - Заказы
- Модели заказов и клиентов
- API для управления заказами

### restaurants/ - Рестораны
- Модели ресторанов и меню
- API для ресторанных данных

## 🧪 Тестирование

Проект включает полный набор тестов:

```bash
# Запуск всех тестов
make test

# Тесты с покрытием кода
make test-cov

# Тесты в Docker
docker compose exec web poetry run pytest
```

Покрытие тестами: **80%+**

## 📚 Документация

Подробная документация находится в папке `docs/`:

- [📖 Основная документация](docs/README.md)
- [🧪 Руководство по тестированию](docs/TESTING.md)
- [🐳 Инструкции по Docker](docs/DOCKER.md)
- [📦 Настройка Poetry](docs/POETRY_SETUP.md)
- [🏗️ Структура документации](docs/STRUCTURE.md)

## 🔧 Команды Make

```bash
# Разработка
make install          # Установка зависимостей
make migrate          # Применение миграций
make run              # Запуск сервера разработки
make shell            # Django shell
make superuser        # Создание суперпользователя

# Тестирование
make test             # Запуск тестов
make test-cov         # Тесты с покрытием

# Docker
make build            # Сборка образов
make up               # Запуск контейнеров
make down             # Остановка контейнеров
make logs             # Просмотр логов
make d-shell          # Shell в контейнере
make d-migrate        # Миграции в контейнере
make d-superuser      # Суперпользователь в контейнере
```

## 🌐 API Endpoints

### Доставки
- `GET/POST /api/deliveries/deliveries/` - список/создание доставок
- `GET/PUT/DELETE /api/deliveries/deliveries/{id}/` - управление доставкой
- `GET /api/deliveries/api/delivery-data/` - данные для отчетов

### Пользователи
- `GET/POST /api/users/users/` - управление пользователями

### Аутентификация
- `GET /api/deliveries/` - страница входа
- `POST /api/deliveries/login/` - авторизация
- `GET /api/deliveries/logout/` - выход

## 🔒 Безопасность

- Аутентификация через Django Session
- CSRF защита
- Валидация данных на уровне моделей
- Безопасная обработка файлов
- Переменные окружения для конфиденциальных данных

## 🚀 Развертывание

### Production

1. Настройте переменные окружения
2. Соберите статические файлы: `python manage.py collectstatic`
3. Настройте веб-сервер (Nginx + Gunicorn)
4. Используйте Docker для контейнеризации

### Docker Production

```bash
# Сборка production образа
docker build -t logiflow:production .

# Запуск с production настройками
docker compose -f docker-compose.prod.yml up -d
```

## 🤝 Вклад в проект

1. Форкните репозиторий
2. Создайте ветку для новой функции
3. Внесите изменения
4. Добавьте тесты
5. Создайте Pull Request

## 📄 Лицензия

Этот проект распространяется под лицензией MIT.

## 📞 Поддержка

- **Issues**: [GitHub Issues](https://github.com/zavet-g/LogiFlow/issues)
- **Документация**: [docs/](docs/)
- **Email**: support@logiflow.com

---

**LogiFlow** - современное решение для управления логистикой доставок 🚚✨ 